<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <form class="row">
    <div class="field col-md-2">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>

<!-- JQuery Chosen gem exercise. remove later: -->
    <%= f.select :name,
      Product.all.map { |u| [u.name, u.id] },
      { include_blank: true },
      { class: 'chosen-select' }
    %>

    <div class="field col-md-2">
      <%= f.label :climate %><br>
      <%= f.text_area :climate %>
    </div>
    <div class="field col-md-2">
      <%= f.label :description %><br>
      <%= f.text_area :description %>
    </div>
    <div class="field col-md-2">
      <%= f.label :image_url %><br>
      <%= f.text_field :image_url %>
    </div>
      <div class="field col-md-2">
      <%= f.label :price %><br>
      <%= f.text_area :price %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
